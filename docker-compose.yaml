services:
  app:
    container_name: anonymous-chatting-frontend
    build:
      context: .
      dockerfile: dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
